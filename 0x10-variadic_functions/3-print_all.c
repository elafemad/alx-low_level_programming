#include "variadic_functions.h"
/**
 * print_all - function that prints anything.
 * @n: list of types of argument
 * Return: 0
 */
void print_all(const char * const format, ...)
{
	va_list p;

	va_start(p, format);

	num = va_args(args, int)

	va_end(args);
}
