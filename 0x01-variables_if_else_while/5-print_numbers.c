#include<stdio.h>
/**
* main - Entry point
*
* Description: 'the program's description'
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int lolla = 0;

	while (lolla < 10)
	{
		printf("%d", lolla);
		lolla++;
	}
	printf("\n");
return (0);
}
