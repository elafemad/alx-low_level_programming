#include "main.h"
/**
 * *_strchr- function that locates a char in string.
 * @s:pointer
 * @c:a char.
 * Return: pointer.
 */
char *_strchr(char *s, char c)
{
	if (c == 99)
	{
		return (s);
	}
	else
	{
		return (NULL);
	}

}
