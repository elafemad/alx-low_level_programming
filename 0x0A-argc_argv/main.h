#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stddef.h>

int _putchar(char c);
int _atoi(char *s);
char *_strcat(char *dest, char *src);
char *_strstr(char *haystack, char *needle);

#endif /* MAIN_H */
