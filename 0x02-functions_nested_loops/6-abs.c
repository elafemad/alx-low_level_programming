#include "main.h"

/**
 * _abs - compute absolute value
 * @n: int input.
 * Description:a function that computes the absolute value of an int    eger.
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

