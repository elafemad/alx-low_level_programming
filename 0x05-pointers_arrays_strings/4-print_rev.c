#include "main.h"
/**
 * print_rev - function print string in reverse.
 * Description: function print string.
 * @s: useless char.
 * Return: void.
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{

	}
	for (; i == 0; --i)
		_putchar(s[i]);
	putchar('\n');
}
