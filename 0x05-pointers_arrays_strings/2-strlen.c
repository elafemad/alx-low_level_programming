#include "main.h"
/**
 * _strlen - function return tthe length of string.
 * Description: function return length.
 * @s: useless char.
 * Return: integer successful.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{

	}
	return (i);
}
